# Postgres (Neon). No Vercel essa variável é criada sozinha ao conectar o Neon.
# Sem ela, o sistema usa um banco local na pasta data/ (bom para testar no computador).
DATABASE_URL=postgres://usuario:senha@host/banco?sslmode=require
