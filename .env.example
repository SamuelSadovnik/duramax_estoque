# Opcional. Conexão do Postgres (Neon).
# Na Vercel NÃO precisa preencher: ao conectar o Neon no Storage a variável é criada sozinha.
# No computador, sem essa variável, o sistema usa um banco local na pasta data/.
# DATABASE_URL=
